#include <stdio.h>
#include "../CommonFunctions/commonFunctions.h"

int main() {
    /*
     * Test for different number of threadss
     * */
    return 0;
}
//    int n = 1000000;
//    int* X = (int*) malloc(n * sizeof(int));
//    int* tmp = (int*) malloc(n * sizeof(int));
//    int i;
//    for (i = 0; i < n; i++) {
//        X[i] = rand();
//    }
//    mergeSort(X, n, tmp);
//    for (i = 0; i < n - 1; i++) {
//        if (X[i] > X[i + 1]) {
//            printf("Error: X[%d] = %d > X[%d] = %d
